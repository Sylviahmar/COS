<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>SignUp</title>
  <style>
    body {
      padding: 0;
      margin: 0;
    }

    .main {
      display: flex;
    }

    .left {
      display: flex;
      justify-content: center;
      align-items: center;
      width: 50vw;
      height: 100vh;
      box-sizing: border-box;
      background-color: #362E21;
      font-size: 50px;
      font-weight: bolder;
      color: black;
    }

    .right {
      width: 50vw; /*  fixed here */
      height: 100vh;
      box-sizing: border-box;
      background-color: black;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .SignUp {
      box-sizing: border-box;
      display: flex;
      flex-direction: column; /*  to stack heading + form */
      align-items: center;
      background-color: #362E21;
      height: 50%;
      width: 350px;
      border-radius: 10px;
      color: rgb(213, 213, 213);
      padding: 20px;
    }

    .form {
      display: flex;
      flex-direction: column;
      gap: 20px;
      margin-top: 20px;
      width: 100%;
      align-items: center;
      justify-content: center;
    }

    input[type="text"],
    input[type="submit"] {
      padding: 10px;
      font-size: 14px;
      width: 80%;
      border: none;
      border-radius: 5px;
    }

    input[type="submit"] {
      background-color: black;
      color: white;
      cursor: pointer;
    }

    input[type="submit"]:hover {
      background-color: #5a5a5a;
    }
    .login{
        color: rgb(233, 225, 225);
    }
    .login:hover{
        color: rgb(255, 255, 255);
    }
  </style>
</head>
<body>
  <div class="main">
    <div class="left">
      <h1>Welcome <br />To <br /> FooDelicious</h1>
    </div>
    <div class="right">
      <div class="SignUp">
        <h1>Sign Up</h1>
        <form class="form" action="/register" method="POST">
            <input   type="text" placeholder="name" name="name"/>
          <input type="text" placeholder="email" name="email" />
          <input type="text" placeholder="password" name="password" />
          <input type="submit" value="Sign Up" name="submit" />
        </form>
        <a class="login" href="/login">already have an account?</a>
      </div>
    </div>
  </div>
</body>
</html>
